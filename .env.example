# OpenRouter (preferred). You may provide comma-separated lists to distribute load across keys/endpoints.
OPENROUTER_API_KEY=sk-or-...
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# Optional OpenRouter attribution headers (recommended)
# OPENROUTER_HTTP_REFERER=https://your.site
# OPENROUTER_X_TITLE=vetra

# OpenAI-compatible aliases (SWRBench uses these names). If you set OPENROUTER_* above, leave these unset.
# OPENAI_API_KEY=sk-or-...
# OPENAI_API_BASE=https://openrouter.ai/api/v1

# Vetra models (reviewer + checker)
VETRA_OPENAI_REVIEW_MODEL=openai/gpt-5.1-codex-max
VETRA_OPENAI_CHECKER_MODEL=openai/gpt-5.1-codex-max

# Optional override base URL (otherwise Vetra uses OPENAI_API_BASE/OPENROUTER_BASE_URL)
# VETRA_OPENAI_BASE_URL=https://openrouter.ai/api/v1

# SWRBench judge model (used by swrbench-1D0E/scripts/run.sh eval if no arg is provided)
SWRBENCH_JUDGE_MODEL=google/gemini-2.5-flash

# SWRBench safety guardrails (see swrbench-1D0E/scripts/run.sh)
SWRBENCH_REQUIRE_ISOLATION=1
SWRBENCH_ALLOW_HOST_RUN=0
SWRBENCH_ALLOW_DOCKER_CLEANUP=0
SWRBENCH_ALLOW_PKILL=0

# Load `.env` automatically when running `npm run bench:swr`.
SWRBENCH_LOAD_DOTENV=1

# When Vetra spawns Docker for SWRBench, mount your SWRBench checkout (the one that contains `data/`).
# If you already have it at `/SWRBench`, you can leave this unset.
# SWRBENCH_HOST_SWRBENCH_DIR=/absolute/path/to/SWRBench

# Optional: explicitly point at the dataset + repos dir (paths are inside the container).
# If you mount SWRBench to `/SWRBench`, these defaults should work.
# SWRBENCH_DATASET_FILE=/SWRBench/data/swr_datasets_0520_d5c5.jsonl
# SWRBENCH_REPOS_DIR=/SWRBench/data/projects

# Local dev convenience: if SWRBench `data/` isn't available, Vetra will generate a small synthetic dataset by default.
# Set SWRBENCH_SYNTHETIC_FALLBACK=0 to require the official dataset.
SWRBENCH_SYNTHETIC_FALLBACK=1
# SWRBENCH_SYNTHETIC_OUT_DIR=.swrbench-synthetic
# SWRBENCH_SYNTHETIC_COUNT=8
# SWRBENCH_SYNTHETIC_SEED=42
# SWRBENCH_SYNTHETIC_CLEAN=1

# Optional: override the Docker image / build settings used by `npm run bench:swr`.
# SWRBENCH_DOCKER_IMAGE=vetra-swrbench-bench:latest
# SWRBENCH_DOCKERFILE=/path/to/vetra/scripts/Dockerfile.swrbench-bench
# SWRBENCH_DOCKER_CONTEXT=/path/to/vetra/scripts
# SWRBENCH_DOCKER_REBUILD=1
