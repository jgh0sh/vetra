{
  "name": "vetra",
  "version": "0.1.0",
  "private": true,
  "description": "Vetra: an MVP PR review harness (Planner-Executor-Verifier)",
  "license": "UNLICENSED",
  "main": "dist/src/index.js",
  "bin": {
    "vetra": "dist/src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "demo": "node scripts/run_demo_with_log.js",
    "bench:swr": "bash scripts/run_swrbench_benchmark.sh",
    "dev": "tsc -w -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.3"
  }
}
